# Copy this file to `.env` and replace the placeholder with your real token.
# Then run the script using the workspace venv.

# Hugging Face token (required)
HUGGINGFACEHUB_API_TOKEN=your_real_huggingface_token_here

# Optional: alternative name
HF_API_TOKEN=your_real_huggingface_token_here
